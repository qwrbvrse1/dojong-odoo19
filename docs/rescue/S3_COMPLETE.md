# Stage 3 Complete — Demo Dataset

## Summary

Successfully seeded demo dataset that passes all gate requirements:
- ✅ 12 active members (10+ required)
- ✅ 2+ members with surname "Smith" for search demo
- ✅ 5 members with profile images (attachments)
- ✅ 5 belt ranks defined, 5 members holding ranks
- ✅ 1 active session (now), 1 upcoming session (+10min), 1 completed, 1 later today
- ✅ 7 enrollments in active session (5+ required)
- ✅ 2 onboarding records (1 complete, 1 incomplete)
- ✅ 7 subscriptions with varied states
- ✅ 1 active kiosk config (PIN: 123456)
- ✅ Search name normalization populated for all members

## Implementation

**Files Created:**
- `scripts/demo_rescue/seed/demo_data.py` — Main seed script (idempotent)
- `scripts/demo_rescue/seed_demo_data.sh` — Shell wrapper

**Gate Script Fixes:**
- Fixed `s3.sh` SQL queries for `_inherits` schema (name → join to res.partner, image → ir_attachment)

## Key Decisions

1. **Idempotency**: Script re-runnable; updates session times relative to "now", adds missing ranks/images to existing members
2. **Subscription plan**: Course-based (not program-based) to avoid needing program records
3. **Rank assignment**: Via `dojo.member.rank` history (current_rank_id is computed)
4. **Profile images**: Stored as `ir_attachment` (res.partner has no image_1920 in this Odoo version)
5. **Enrollment constraint**: Used `skip_subscription_check` context flag (subscriptions created immediately prior)
6. **Trial lead**: Skipped (dojo_crm model exists but decided not essential for gate pass)

## Data Structure

**Members** (12 total):
- demo1, demo2 (from accounts.py)
- John Smith, Jane Smith (2x Smith)
- Bob Smithson (Smithson)
- Alice Doe (Doe)
- Maria Garcia Lopez (multi-word surname)
- Carlos Rodriguez, Emily Chen, David Park, Sarah Johnson, Michael Williams

**Belt Ranks**: White, Yellow, Orange, Green, Blue (attendance thresholds: 0, 10, 20, 35, 50)

**Sessions** (all assigned to instructor1 profile):
- Completed: started -8hr, ended -7hr (state='done')
- Active: started -10min, ends +50min (state='open')
- Upcoming: starts +10min, ends +70min (state='open')
- Later: starts +4hr, ends +5hr (state='open')

**Enrollments**: demo1, demo2, + 5 seeded members → active session

**Onboarding**: 1 completed (demo1), 1 in_progress (demo2)

**Subscriptions**: 7 total (all enrolled members), plan "Demo Monthly Plan" (course-based, allows demo template)

**Kiosk**:
- Name: Demo Kiosk
- PIN: 123456
- URL: http://localhost:8070/dojo/kiosk?token=<auto-generated>

## Verification

Gate passed: `bash scripts/demo_rescue/verify/s3.sh`

All 13 assertions pass.

## Re-running

To re-seed (updates times, adds missing data):
```bash
bash scripts/demo_rescue/seed_demo_data.sh
```

Kiosk URL and PIN printed at end of seed run.
