## Stage 1 — ACL closure + parent tightening (USABILITY_PASS.md changes 1–2)

Goal: `bash scripts/usability_pass/verify/s1.sh` passes. Read the gate FIRST — it asserts:
- NO active `ir.model.access` row grants `base.group_user` write/create/unlink on ANY `dojo.*`
  model (the gate query is generic across all dojo models, not just the three known files).
- Parent group (`dojo_core.group_dojo_parent_student`) has NO create/unlink on
  `dojo.class.enrollment` and `dojo.course.auto.enroll`, and NO unlink on
  `dojo.emergency.contact`.
- A probe internal user (`probe@qa.local` / `Probe-2026!`, NO dojo groups) authenticates but
  gets an AccessError reading `dojo.member`.
- Admin and instructor Playwright smokes still pass; parent portal `/my/dojo` still returns 200.

Implementation requirements:
1. Demote/remove `base.group_user` rows in `addons/dojo_base/security/ir.model.access.csv`,
   `addons/dojo_classes/security/ir.model.access.csv`,
   `addons/dojo_attendance/security/ir.model.access.csv` — then grep ALL addons for any other
   `base.group_user` rows on `dojo.*` models and demote those too. For every demoted row,
   confirm equivalent group-scoped rows exist (admin RWX; instructor/parent per the matrix in
   `addons/dojo_core/security/ir.model.access.csv`). Read-only `base.group_user` rows may stay
   ONLY where a core widget genuinely needs them (prefer none; document any you keep).
2. Tighten the parent rows in `dojo_core`'s CSV as the gate requires. Then fix the portal
   controllers (`addons/dojo_members_portal/controllers/main.py`): `/my/dojo/enroll`,
   `/my/dojo/unenroll`, `/my/dojo/auto-enroll` must keep working by switching the write to
   `sudo()` AFTER the existing household validation (landmine 3). Verify manually with the
   parent login before running the gate.
3. Integration accounts: query `res_users` for active internal users outside the dojo groups
   (the n8n account at minimum). Grant them an explicit group via an idempotent seed/data step.
   Record who got what in `docs/usability_pass/S1_PLAN.md`.
4. Seed the probe user with an idempotent script `scripts/usability_pass/seed/probe_user.py`
   (odoo shell pattern; upsert by login; internal user, NO dojo groups, no admin rights).
5. Re-run the module upgrade for every module whose CSV you touched, restart web.

Done = gate passes + commit.
