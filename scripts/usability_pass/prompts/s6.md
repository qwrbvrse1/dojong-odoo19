## Stage 6 — Parent portal checklist + FREEZE (change 9 + final proof)

Goal: `bash scripts/usability_pass/verify/s6.sh` passes. That gate cold-restarts the stack
(`docker compose down && up`) and re-runs EVERY prior gate against the fresh containers, then
checks the new portal endpoint and the runbook.

Your tasks:
1. **Parent onboarding checklist.** New authenticated route
   `GET /my/dojo/onboarding/summary` (http route, `auth='user'`, returns JSON) in
   `addons/dojo_members_portal/controllers/main.py`: household-scoped via the existing
   `_get_household_member_ids()` helpers; per child returns `member_id`, `name`,
   `progress_pct`, `steps` (key/label/complete from the S5 lifecycle set), and
   `missing_steps`. A student login sees only their own record. Add a visible checklist block
   to the `/my/dojo` home tab template rendering the same data — this closes the loop with the
   kiosk's "Send Reminder" action (the SMS finally has a destination page).
2. **Runbook.** Write `USABILITY_PASS_RUNBOOK.md` in repo root: what changed per stage (one
   line each), the new endpoint names + params (`instructor_key`, `mark_remaining_absent`,
   `/my/dojo/onboarding/summary`), how to rotate a kiosk token, which integration accounts
   were granted which groups in S1, the probe account, and any deliberately deferred items.
3. Run the gate yourself. The cold restart is the point — if anything only worked because of
   warm state (un-upgraded module, manual tweak in a running container, uncommitted seed), it
   will fail here. Fix forward by making the module/seed scripts own that state.
4. `git add -A && git commit`, then `git tag -f upass-ready`.

Done = gate passes + commit + tag.
