## Stage 3 — Backend surname search view + dashboard stub removal (changes 5–6)

Goal: `bash scripts/usability_pass/verify/s3.sh` passes. Read the gate FIRST. It asserts:
- A search view exists for `dojo.member` whose arch references `last_name` (field + filter
  and/or group-by).
- The `dojo.member` list view arch references `last_name` (visible column).
- `addons/dojo_instructor_dashboard` is either deleted or contains a valid
  `__manifest__.py`, and `ir_module_module` has no broken entry for it.
- Admin Playwright smoke still passes (view XML errors surface here).

Implementation requirements:
1. `addons/dojo_core/views/member_views.xml` currently has NO search view. Add one
   (id `view_dojo_member_search`): `name` field with a `filter_domain` that also matches
   `last_name` and `search_name_normalized`; a dedicated `last_name` field entry; filters for
   active membership states; `group_by` options for Last Name and Membership State. Wire it to
   the member action(s). The fields already exist, stored and indexed
   (`addons/dojo_core/models/member.py` L79/L87/L95) — this is view-layer work only.
2. Add `last_name` (and `first_name`, optional) to the member list view after `name`, and set
   the list's `default_order="last_name, first_name"` on the list arch (do NOT change the
   model's `_order` — that ripples into kiosk payload ordering).
3. Remove the vestigial `addons/dojo_instructor_dashboard` directory (no `__manifest__.py` —
   landmine 6). First check `ir_module_module` state; if a stale row exists, clean it
   (`state='uninstalled'` rows are fine to leave; broken `to upgrade/to install` rows are not).
   The real instructor actions in `addons/dojo_core/views/dojo_instructor_dashboard_views.xml`
   stay — do not touch them.
4. Upgrade `dojo_core`, restart web before gating.

Done = gate passes + commit.
