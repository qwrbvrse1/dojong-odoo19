## Stage 0 — Baseline (USABILITY_PASS.md Stage map S0)

Goal: `bash scripts/usability_pass/verify/s0.sh` passes — the module upgrade across every
module this pass will touch runs with zero ERROR/CRITICAL/Traceback, no modules are stuck,
the web client answers 200, and all five demo accounts authenticate.

This stage makes NO functional changes. It proves the post-rescue baseline is intact in THIS
worktree before any hardening begins. If the gate already passes on first run, commit an empty
checkpoint and stop.

ANALYZE first:
- Run the gate once to capture the real state before changing anything.
- If the upgrade fails, suspects are the rescue-era landmines (USABILITY_PASS.md "Known
  landmines" 5 and 8): manifest data ordering and pg_trgm.
- If accounts fail to authenticate, re-run the rescue's account seed
  (`scripts/demo_rescue/seed/` if present in this worktree) — do not invent new accounts.

Done = gate passes + commit.
