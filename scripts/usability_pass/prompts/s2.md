## Stage 2 — Kiosk pre-PIN gating, token rotation, action log (changes 3–4)

Goal: `bash scripts/usability_pass/verify/s2.sh` passes. Read the gate FIRST. It mandates
exact names — these are the contract:

- **Pre-PIN minimal data.** `/kiosk/member/profile` WITHOUT instructor auth must return only:
  `member_id`, `name`, `image_url`, `belt_rank`, `belt_color`, `attendance_state`, and the
  member's enrolled sessions for today. It must NOT contain `date_of_birth`, `email`, `phone`,
  `household`, `guardians`, `credit_balance`, `billing_failure_count`, or `workflow_status`.
  `/kiosk/search` pre-PIN stays as-is (name/belt/photo only — it already is minimal).
- **Instructor session key.** `/kiosk/auth/pin` (valid PIN) must return an `instructor_key`
  (random, per-unlock, expiring ≤ 8h, stored server-side — a field or model tied to
  `dojo.kiosk.config` is fine). Privileged endpoints (`/kiosk/member/profile` full payload,
  every `/kiosk/instructor/*` route) require a valid `instructor_key` param and must return a
  JSON error containing the string `instructor_auth_required` when it is missing/invalid.
- **Self-check-in keeps working pre-PIN**: `/kiosk/checkin` must NOT require the key.
- **Token rotation.** Add `action_rotate_token()` on `dojo.kiosk.config` (regenerates
  `kiosk_token`, logs to chatter) + a button on the config form view.
- **Action log.** New model `dojo.kiosk.action.log` (config_id, action, member_id, session_id,
  is_instructor_action, summary, create_date). Every kiosk mutation (checkin, checkout,
  attendance mark, roster add/remove, onboarding action) writes one row. Admin-only menu/view.

Implementation notes:
- All service logic lives in `addons/dojo_kiosk/models/dojo_kiosk_service.py`; routes in
  `controllers/kiosk_controller.py`. Thread `instructor_key` through the existing
  `_guard_token()` pattern — add a `_guard_instructor(token, instructor_key)` helper.
- Update `static/src/kiosk_app.js` (landmine 4): pre-PIN profile modal renders the minimal
  card; after PIN unlock store the key in app state and re-fetch profiles; pass the key on all
  instructor calls. Bump the asset version / restart web so the SPA reloads.
- The kiosk PIN is 123456 (demo seed) — do not change it; the gate uses it.
- The gate's shell test calls `env['dojo.kiosk.service'].sudo().mark_attendance(session_id,
  member_id, 'present')` and expects a `dojo.kiosk.action.log` row afterward. If the service's
  attendance method has a different name, add `mark_attendance` as a thin alias — do not rename
  the existing method.
- Upgrade `dojo_kiosk`, restart web before gating.

Done = gate passes + commit.
