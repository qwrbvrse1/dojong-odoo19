## Stage 0 — Boot clean (DEMO_RESCUE.md Phase 0 + "Known landmines")

Goal: `bash scripts/demo_rescue/verify/s0.sh` passes — the module upgrade (including
`dojo_credits`) runs with zero ERROR/CRITICAL/Traceback, no modules are stuck, and the web
client answers 200.

ANALYZE first:
- `DEMO_RESCUE.md` — "Known landmines" 1, 2, 3, 7 are the prime suspects.
- `git diff main...HEAD --stat` and the uncommitted changes imported into this worktree.
- Start the stack (`docker compose up -d db web`), tail `docker compose logs web`, and run the
  gate once to capture the real failure before changing anything.

Likely fixes (verify before applying):
- Manifest `data` ordering in `dojo_core`: `dojo_core_menus.xml` references
  `action_all_sessions_today` defined in `dojo_instructor_dashboard_views.xml` — the action must
  load before the menu. If untangling is slow, REVERT the menu repointing entirely; the demo does
  not depend on it.
- `dojo_core.group_dojo_instructor` defined with `implied_ids` ops in BOTH
  `security/dojo_security.xml` and `data/instructor_todos_data.xml` (with `(3, ref(...))` unlinks
  on `account.*`/`hr.*` refs). Consolidate or guard so the upgrade cannot die on a missing ref.
- If the upgrade dies inside any other file Codex touched, prefer reverting that file to `main`'s
  version over patching forward.

Done = gate passes + commit.
