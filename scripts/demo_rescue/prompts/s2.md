## Stage 2 — Admin & instructor UI loads clean (DEMO_RESCUE.md Phase 1)

Goal: `bash scripts/demo_rescue/verify/s2.sh` passes — a real Playwright browser logs in as
admin@demo.com AND instructor1@demo.com with zero JS errors / zero 5xx, admin sees ≥6 root menus
including Kiosk.

The client's reported symptom lives here: "errors in the UI, most modules don't load on admin."

ANALYZE first:
- Run the gate once; read `/tmp/rescue_smoke_*.png` screenshots and the gate's error lines.
- `docker compose logs --tail=200 web` for server-side tracebacks triggered by page loads.
- Suspects from DEMO_RESCUE.md landmines: #4 (access CSVs demoted `base.group_user` rows in
  `dojo_credits`/`dojo_onboarding` — views readable by plain users now throw), menu group_ids
  overrides in `dojo_credits`/`dojo_onboarding` views, and any web asset/Owl error from
  `dojo_kiosk/static/src/kiosk_app.js`.

Rules of engagement:
- Fix the root cause when it is small; otherwise revert the offending Codex change to `main`'s
  version. The demo needs a clean UI more than it needs Codex's security hardening — EXCEPT keep
  instructors out of Accounting menus if that is cheap to preserve.
- After any access/security/view change: re-run the module upgrade for the touched modules and
  restart web before re-running the gate (stale registry = false results).

Done = gate passes + commit.
