## Stage 1 — Demo accounts (DEMO_RESCUE.md Phase 2)

Goal: `bash scripts/demo_rescue/verify/s1.sh` passes — all five client-agreed accounts
authenticate via JSON-RPC and instructor1 has a linked `dojo.instructor.profile`.

| Role | Login | Password | Requirements |
|---|---|---|---|
| Admin | admin@demo.com | admin123 | `dojo_core.group_dojo_admin` + internal user |
| Instructor | instructor1@demo.com | dojo@2026 | `group_dojo_instructor` + `dojo.instructor.profile` with `user_id` set |
| Student 1 | demo1@demo.com | dojo@2026 | dojo.member + portal/internal login |
| Student 2 | demo2@demo.com | dojo@2026 | dojo.member + portal/internal login |
| Parent | DemoParent@demo.com | dojo@2026 | parent group, household-linked to BOTH students |

Implementation: extend or replace `scripts/prep_kiosk_demo.sh`'s account section with an
idempotent seed (odoo shell script under `scripts/demo_rescue/seed/accounts.py` run via the
odoo-bin shell pattern is fine). Upsert by login — never duplicate. Students must be linked to
their member records; parent linked via the household/guardian mechanism used by `dojo_core`
(read `addons/dojo_core/models/member.py` household fields before guessing).

Note: student/parent logins may authenticate as portal users — that is fine; the gate only
requires successful authentication and the instructor profile link.

Done = gate passes + commit.
