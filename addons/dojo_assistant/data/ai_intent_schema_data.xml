<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <!--
        AI Intent Schema Seed Data
        
        Defines the available AI assistant intents, their parameters,
        role permissions, and confirmation requirements.
    -->

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Read-Only Intents (Auto-Execute)                                     -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_member_lookup" model="dojo.ai.intent.schema">
        <field name="intent_type">member_lookup</field>
        <field name="name">Member Lookup</field>
        <field name="description">Look up member information by name or ID</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member to look up"},
        "member_id": {"type": "integer", "description": "ID of the member to look up"}
    }
}</field>
        <field name="example_phrases">Look up John Smith
Find member Sarah
What's Test8's information?
Show me details for Mike
Who is Jane Doe?</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_class_list" model="dojo.ai.intent.schema">
        <field name="intent_type">class_list</field>
        <field name="name">Class List</field>
        <field name="description">List classes/sessions for a given date</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "date": {"type": "string", "format": "date", "description": "Date to list classes for (YYYY-MM-DD)"}
    }
}</field>
        <field name="example_phrases">What classes are on today?
List all classes for Friday
Show me Monday's classes
What sessions do we have this week?</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_schedule_today" model="dojo.ai.intent.schema">
        <field name="intent_type">schedule_today</field>
        <field name="name">Today's Schedule</field>
        <field name="description">Get today's class schedule</field>
        <field name="category">read</field>
        <field name="parameters_schema">{}</field>
        <field name="example_phrases">What's the schedule today?
Show today's classes
What classes are happening now?
What's on the schedule?
What do we have today?</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_belt_lookup" model="dojo.ai.intent.schema">
        <field name="intent_type">belt_lookup</field>
        <field name="name">Belt Lookup</field>
        <field name="description">Look up a member's current belt rank. Use member_name to get a specific member's rank; omit it to list all belt ranks.</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member whose belt rank to look up"},
        "member_id": {"type": "integer", "description": "ID of the member whose belt rank to look up"},
        "belt_name": {"type": "string", "description": "Name of a specific belt rank to look up (omit to get member's rank)"}
    }
}</field>
        <field name="example_phrases">What belt is John on?
Show me Sarah's belt rank
What rank is Test8?
What color belt does Mike have?
What belt does john smith have?
Show all belt ranks</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_subscription_lookup" model="dojo.ai.intent.schema">
        <field name="intent_type">subscription_lookup</field>
        <field name="name">Subscription Lookup</field>
        <field name="description">Look up a member's subscription status</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"}
    }
}</field>
        <field name="example_phrases">What's John's subscription status?
Is Sarah's membership active?
Check Test8's plan
Does Mike have an active subscription?
What plan is Jane on?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_attendance_history" model="dojo.ai.intent.schema">
        <field name="intent_type">attendance_history</field>
        <field name="name">Attendance History</field>
        <field name="description">Get attendance history for a member</field>
        <field name="category">read</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "limit": {"type": "integer", "description": "Max records to return", "default": 10}
    }
}</field>
        <field name="example_phrases">Show John's attendance history
How often has Sarah been coming in?
When did Test8 last attend?
Show me Mike's recent classes
What's Jane's attendance like?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Attendance Intents                                                   -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_attendance_checkin" model="dojo.ai.intent.schema">
        <field name="intent_type">attendance_checkin</field>
        <field name="name">Check In Member</field>
        <field name="description">Check in a member for attendance</field>
        <field name="category">attendance</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member to check in"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "session_id": {"type": "integer", "description": "Optional session ID"}
    },
    "required": ["member_name"]
}</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Check in John
Mark Sarah as present
Test8 just arrived
Sign in Mike for class
Check in Jane Doe</field>
        <field name="confirmation_template">Check in {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <record id="intent_attendance_checkout" model="dojo.ai.intent.schema">
        <field name="intent_type">attendance_checkout</field>
        <field name="name">Check Out Member</field>
        <field name="description">Check out a member from attendance</field>
        <field name="category">attendance</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member to check out"},
        "member_id": {"type": "integer", "description": "ID of the member"}
    },
    "required": ["member_name"]
}</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Check out John
Mark Sarah as done for today
Test8 is leaving
Sign out Mike
Jane is heading home</field>
        <field name="confirmation_template">Check out {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Enrollment Intents                                                   -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_member_enroll" model="dojo.ai.intent.schema">
        <field name="intent_type">member_enroll</field>
        <field name="name">Enroll Member</field>
        <field name="description">Enroll a member in a class session</field>
        <field name="category">enrollment</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member to enroll"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "class_name": {"type": "string", "description": "Name of the class"},
        "session_id": {"type": "integer", "description": "ID of the session"}
    },
    "required": ["member_name", "class_name"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Enroll John in today's Advanced Sparring
Sign Sarah up for the 6pm Fundamentals session
Add Test8 to this morning's class
Book Mike into the Kids Karate session today
Enroll Jane in the next available class</field>
        <field name="confirmation_template">Enroll {member_name} in {class_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <record id="intent_member_unenroll" model="dojo.ai.intent.schema">
        <field name="intent_type">member_unenroll</field>
        <field name="name">Unenroll Member</field>
        <field name="description">Remove a member from a class session</field>
        <field name="category">enrollment</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member to unenroll"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "class_name": {"type": "string", "description": "Name of the class"},
        "session_id": {"type": "integer", "description": "ID of the session"}
    },
    "required": ["member_name"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Remove John from today's class
Unenroll Sarah from Advanced Sparring
Take Test8 off the session
Cancel Mike's enrollment
Drop Jane from this morning's class</field>
        <field name="confirmation_template">Remove {member_name} from {class_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Belt Progression Intents                                             -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_belt_promote" model="dojo.ai.intent.schema">
        <field name="intent_type">belt_promote</field>
        <field name="name">Promote Belt Rank</field>
        <field name="description">Promote a member to a new belt rank</field>
        <field name="category">belt</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member to promote"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "new_belt": {"type": "string", "description": "Name of the new belt rank"}
    },
    "required": ["member_name", "new_belt"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Promote John to blue belt
Advance Sarah to the next rank
Test8 earned their green stripe
Move Mike up to purple belt
Promote Jane to her next belt level</field>
        <field name="confirmation_template">Promote {member_name} to {new_belt}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Subscription Intents                                                 -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_subscription_create" model="dojo.ai.intent.schema">
        <field name="intent_type">subscription_create</field>
        <field name="name">Create Subscription</field>
        <field name="description">Create a new subscription for a member</field>
        <field name="category">subscription</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "plan_name": {"type": "string", "description": "Name of the subscription plan"},
        "plan_id": {"type": "integer", "description": "ID of the subscription plan"},
        "start_date": {"type": "string", "format": "date", "description": "Start date (YYYY-MM-DD)"},
        "end_date": {"type": "string", "format": "date", "description": "End date (YYYY-MM-DD, optional)"},
        "note": {"type": "string", "description": "Optional notes"}
    },
    "required": ["member_name", "plan_name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create a monthly subscription for John
Sign Sarah up for the unlimited plan
Add Test8 to the family membership
Set up a yearly plan for Mike
Start a new subscription for Jane</field>
        <field name="confirmation_template">Create {plan_name} subscription for {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_subscription_cancel" model="dojo.ai.intent.schema">
        <field name="intent_type">subscription_cancel</field>
        <field name="name">Cancel Subscription</field>
        <field name="description">Cancel a member's subscription</field>
        <field name="category">subscription</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"}
    },
    "required": ["member_name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Cancel John's subscription
End Sarah's membership
Stop Test8's plan
Terminate Mike's subscription
Cancel Jane's membership plan</field>
        <field name="confirmation_template">Cancel subscription for {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Communication Intents                                                -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_contact_parent" model="dojo.ai.intent.schema">
        <field name="intent_type">contact_parent</field>
        <field name="name">Contact Parent/Guardian</field>
        <field name="description">Send a message to a member's guardian</field>
        <field name="category">communication</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "subject": {"type": "string", "description": "Email subject line"},
        "body": {"type": "string", "description": "Message body text"},
        "message": {"type": "string", "description": "Alternative for body"},
        "send_email": {"type": "boolean", "description": "Send via email", "default": true},
        "send_sms": {"type": "boolean", "description": "Send via SMS", "default": true}
    },
    "required": ["member_name", "body"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Message John's parent about his progress
Email Sarah's guardian about the upcoming test
Send a note to Test8's mom
Contact Mike's parents about his attendance
Let Jane's guardian know about the schedule change</field>
        <field name="confirmation_template">Send message to {member_name}'s guardian about "{subject}"?</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Member Management Intents                                            -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_member_create" model="dojo.ai.intent.schema">
        <field name="intent_type">member_create</field>
        <field name="name">Create Member</field>
        <field name="description">Create a new member record</field>
        <field name="category">member</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "Full name of the new member"},
        "email": {"type": "string", "format": "email", "description": "Email address"},
        "phone": {"type": "string", "description": "Phone number"}
    },
    "required": ["name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create a new member named John Smith
Add a new student Sarah Jones
Register new member Test8
Create a profile for Mike Williams
Add a new student Jane Doe with email jane@example.com</field>
        <field name="confirmation_template">Create new member "{name}"?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_member_update" model="dojo.ai.intent.schema">
        <field name="intent_type">member_update</field>
        <field name="name">Update Member</field>
        <field name="description">Update an existing member's information</field>
        <field name="category">member</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member to update"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "name": {"type": "string", "description": "New name"},
        "email": {"type": "string", "format": "email", "description": "New email"},
        "phone": {"type": "string", "description": "New phone"}
    },
    "required": ["member_name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Update John's email to john@example.com
Change Sarah's phone number
Update Test8's name
Edit Mike's contact information
Update Jane's profile details</field>
        <field name="confirmation_template">Update {member_name}'s profile?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Class Management Intents                                             -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_class_create" model="dojo.ai.intent.schema">
        <field name="intent_type">class_create</field>
        <field name="name">Create Class Session</field>
        <field name="description">Create a new class session</field>
        <field name="category">class</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "class_name": {"type": "string", "description": "Name of the class template"},
        "template_id": {"type": "integer", "description": "ID of the class template"},
        "start_datetime": {"type": "string", "format": "date-time", "description": "Start date/time (ISO 8601)"}
    },
    "required": ["class_name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create a new Advanced Sparring session
Add a class session for Kids Karate tomorrow
Schedule a new Fundamentals class for 6pm
Create an extra session for Adult BJJ on Friday</field>
        <field name="confirmation_template">Create new "{class_name}" session?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_class_cancel" model="dojo.ai.intent.schema">
        <field name="intent_type">class_cancel</field>
        <field name="name">Cancel Class Session</field>
        <field name="description">Cancel a scheduled class session</field>
        <field name="category">class</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "class_name": {"type": "string", "description": "Name of the class"},
        "session_id": {"type": "integer", "description": "ID of the session to cancel"}
    }
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Cancel today's Advanced Sparring
Cancel the 6pm Fundamentals class
Call off the Kids Karate session
Cancel this morning's class
Take the Adult BJJ session off the schedule</field>
        <field name="confirmation_template">Cancel the "{class_name}" session?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- System Intents                                                       -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_undo_action" model="dojo.ai.intent.schema">
        <field name="intent_type">undo_action</field>
        <field name="name">Undo Action</field>
        <field name="description">Undo the most recent undoable action</field>
        <field name="category">system</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "original_action_log_id": {"type": "integer", "description": "ID of the action to undo"}
    }
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Undo that
Undo the last action
Revert what I just did
Go back on that
That was a mistake, undo it</field>
        <field name="confirmation_template">Undo the previous action?</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_unknown" model="dojo.ai.intent.schema">
        <field name="intent_type">unknown</field>
        <field name="name">Unknown Intent</field>
        <field name="description">Unrecognized or conversational query</field>
        <field name="category">system</field>
        <field name="parameters_schema">{}</field>
        <field name="allowed_roles">kiosk,instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Course Roster Intents                                                -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_course_enroll" model="dojo.ai.intent.schema">
        <field name="intent_type">course_enroll</field>
        <field name="name">Enroll in Course Roster</field>
        <field name="description">Add a member to a course's permanent roster, allowing them to join any session of that class. This is different from member_enroll which reserves a single session spot.</field>
        <field name="category">enrollment</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "class_name": {"type": "string", "description": "Name of the class/course template to join the roster of"}
    },
    "required": ["member_name", "class_name"]
}</field>
        <field name="example_phrases">Add Test8 to the Advanced Sparring roster
Add John to the Adult Fundamentals course
Enroll Sarah in the Kids Karate course roster
Put Mike on the course roster for Intermediate BJJ</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="confirmation_template">Add {member_name} to the {class_name} course roster?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- Belt Test Intents                                                    -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_belt_test_register" model="dojo.ai.intent.schema">
        <field name="intent_type">belt_test_register</field>
        <field name="name">Register for Belt Test</field>
        <field name="description">Register a member for an upcoming belt test event, specifying the target belt rank they are testing for</field>
        <field name="category">belt</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "target_belt": {"type": "string", "description": "Belt rank they are testing for (e.g. blue belt, green stripe)"},
        "test_date": {"type": "string", "format": "date", "description": "Date of the belt test (YYYY-MM-DD), optional — defaults to the next scheduled test"},
        "test_name": {"type": "string", "description": "Name of the belt test event, optional"}
    },
    "required": ["member_name", "target_belt"]
}</field>
        <field name="example_phrases">Register Test8 for a blue belt test
Schedule a belt test for John testing for green belt
Add Sarah to the next belt test for purple belt
Sign Mike up for his yellow stripe belt test</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="confirmation_template">Register {member_name} for a belt test (testing for {target_belt})?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ── Extended Intents ──────────────────────────────────────────────── -->

    <record id="intent_at_risk_members" model="dojo.ai.intent.schema">
        <field name="intent_type">at_risk_members</field>
        <field name="name">At-Risk Members</field>
        <field name="description">Find active members who have not attended in a given number of days</field>
        <field name="category">member</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "days": {"type": "integer", "description": "Number of days of inactivity threshold (default 14)"}
    },
    "required": []
}</field>
        <field name="example_phrases">Who hasn't been in for 2 weeks?
Show me at-risk members
Which members are inactive?
Who hasn't come in lately?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_campaign_lookup" model="dojo.ai.intent.schema">
        <field name="intent_type">campaign_lookup</field>
        <field name="name">Campaign Stats Lookup</field>
        <field name="description">Look up recent marketing campaign statistics including how many people were reached</field>
        <field name="category">marketing</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "campaign_name": {"type": "string", "description": "Name of a specific campaign to look up, optional"}
    },
    "required": []
}</field>
        <field name="example_phrases">How many people did we reach last campaign?
Show me campaign stats
How many emails went out this week?
What's the status of our campaigns?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_marketing_card_lookup" model="dojo.ai.intent.schema">
        <field name="intent_type">marketing_card_lookup</field>
        <field name="name">Marketing Card Lookup</field>
        <field name="description">List marketing cards currently published on the kiosk</field>
        <field name="category">marketing</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {},
    "required": []
}</field>
        <field name="example_phrases">What marketing cards are on the kiosk?
Show me active marketing cards
What's being shown on the kiosk right now?</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">False</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_subscription_pause" model="dojo.ai.intent.schema">
        <field name="intent_type">subscription_pause</field>
        <field name="name">Pause Subscription</field>
        <field name="description">Pause a member's active subscription</field>
        <field name="category">subscription</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"}
    },
    "required": ["member_name"]
}</field>
        <field name="example_phrases">Pause Alex's membership
Put Sarah's subscription on hold
Suspend John's membership temporarily</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="confirmation_template">Pause {member_name}'s active subscription?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_subscription_resume" model="dojo.ai.intent.schema">
        <field name="intent_type">subscription_resume</field>
        <field name="name">Resume Subscription</field>
        <field name="description">Resume a member's paused subscription</field>
        <field name="category">subscription</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"}
    },
    "required": ["member_name"]
}</field>
        <field name="example_phrases">Resume Alex's membership
Reactivate Sarah's subscription
Unpause John's membership</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="confirmation_template">Resume {member_name}'s paused subscription?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_campaign_create" model="dojo.ai.intent.schema">
        <field name="intent_type">campaign_create</field>
        <field name="name">Create Campaign</field>
        <field name="description">Create a new marketing campaign targeting members</field>
        <field name="category">marketing</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "campaign_name": {"type": "string", "description": "Name of the campaign"},
        "send_email": {"type": "boolean", "description": "Whether to send emails (default true)"},
        "send_sms": {"type": "boolean", "description": "Whether to send SMS (default false)"}
    },
    "required": ["campaign_name"]
}</field>
        <field name="example_phrases">Create a campaign for all active members
Set up a summer camp email blast
Create a new SMS campaign called Back to School</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="confirmation_template">Create a new campaign called '{campaign_name}'?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_campaign_activate" model="dojo.ai.intent.schema">
        <field name="intent_type">campaign_activate</field>
        <field name="name">Activate Campaign</field>
        <field name="description">Activate a draft marketing campaign so it will send on the next scheduled run</field>
        <field name="category">marketing</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "campaign_name": {"type": "string", "description": "Name of the campaign to activate"}
    },
    "required": []
}</field>
        <field name="example_phrases">Send the summer camp campaign now
Activate the welcome campaign
Launch our back to school campaign</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="confirmation_template">Activate campaign '{campaign_name}' and queue it for sending?</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_social_post_schedule" model="dojo.ai.intent.schema">
        <field name="intent_type">social_post_schedule</field>
        <field name="name">Schedule Social Post</field>
        <field name="description">Schedule a social media post to be published at a future date and time</field>
        <field name="category">social</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "message": {"type": "string", "description": "The post message text"},
        "scheduled_date": {"type": "string", "format": "date-time", "description": "When to publish (ISO 8601)"},
        "platform": {"type": "string", "enum": ["facebook", "instagram"], "description": "Platform to post to"}
    },
    "required": ["message", "scheduled_date"]
}</field>
        <field name="example_phrases">Schedule a post for next Monday about belt testing
Queue a Facebook post for Friday about summer camp
Schedule our promotion post for Saturday morning</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="confirmation_template">Schedule post '{message}' for {scheduled_date}?</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- NEW CRUD Intents (Generic Create/Update/Delete Operations)          -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_program_create" model="dojo.ai.intent.schema">
        <field name="intent_type">program_create</field>
        <field name="name">Create Program</field>
        <field name="description">Create a new dojo program (e.g., Kids Karate Gold, Adult BJJ)</field>
        <field name="category">class</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "Name of the new program"},
        "description": {"type": "string", "description": "Program description (optional)"},
        "active": {"type": "boolean", "description": "Whether the program is active", "default": true}
    },
    "required": ["name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create a new program called Adult Kickboxing
Add a program for Competition Team
Create the Tiny Titans program for toddlers</field>
        <field name="confirmation_template">Create new program "{name}"?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_belt_test_register_crud" model="dojo.ai.intent.schema">
        <field name="intent_type">belt_test_register_crud</field>
        <field name="name">Register for Belt Test (CRUD)</field>
        <field name="description">Register a member for a belt test by creating a member.rank record</field>
        <field name="category">belt</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member testing"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "rank_name": {"type": "string", "description": "Name of the belt rank being tested for"},
        "program_name": {"type": "string", "description": "Name of the program"},
        "date_awarded": {"type": "string", "format": "date", "description": "Date awarded (YYYY-MM-DD)"},
        "notes": {"type": "string", "description": "Optional notes"}
    },
    "required": ["member_name", "rank_name"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Register John for a blue belt test
Sign Sarah up to test for green stripe
Add Test8 to the purple belt test</field>
        <field name="confirmation_template">Register {member_name} for belt test?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_class_template_create" model="dojo.ai.intent.schema">
        <field name="intent_type">class_template_create</field>
        <field name="name">Create Class Template</field>
        <field name="description">Create a new class template (e.g., Fundamentals, Advanced Sparring)</field>
        <field name="category">class</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "Name of the class template"},
        "program_name": {"type": "string", "description": "Name of the program this class belongs to"},
        "max_capacity": {"type": "integer", "description": "Max students per session", "default": 20},
        "level": {"type": "string", "description": "Class difficulty level"},
        "duration_minutes": {"type": "integer", "description": "Duration in minutes", "default": 60},
        "active": {"type": "boolean", "description": "Whether the template is active", "default": true}
    },
    "required": ["name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create a class template called Fundamentals for the Kids Program
Add a new Sparring class to Adult BJJ
Create the Advanced Forms class template</field>
        <field name="confirmation_template">Create class template "{name}" under {program_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_class_enrollment_create" model="dojo.ai.intent.schema">
        <field name="intent_type">class_enrollment_create</field>
        <field name="name">Enroll in Class Session</field>
        <field name="description">Enroll a member in a specific class session</field>
        <field name="category">enrollment</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "session_id": {"type": "integer", "description": "ID of the class session"},
        "status": {"type": "string", "description": "Status of the enrollment", "default": "registered"}
    },
    "required": ["member_name", "session_id"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Enroll John in today's class
Add Sarah to the 6pm session
Sign up Test8 for this morning's class
Put Mike in the Friday evening session</field>
        <field name="confirmation_template">Enroll {member_name} in the session?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <record id="intent_class_enrollment_cancel" model="dojo.ai.intent.schema">
        <field name="intent_type">class_enrollment_cancel</field>
        <field name="name">Cancel Class Enrollment</field>
        <field name="description">Remove a member from a specific class session</field>
        <field name="category">enrollment</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "member_id": {"type": "integer", "description": "ID of the member"},
        "session_id": {"type": "integer", "description": "ID of the class session"}
    },
    "required": ["member_name"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Remove John from today's class
Cancel Sarah's enrollment in the 6pm session
Drop Test8 from this morning's class
Unenroll Mike from Friday's session</field>
        <field name="confirmation_template">Cancel {member_name}'s enrollment?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- EXTENDED CRUD Intents (Belt Tests, Attendance, Credits, Marketing)   -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_belt_test_create" model="dojo.ai.intent.schema">
        <field name="intent_type">belt_test_create</field>
        <field name="name">Create Belt Test Event</field>
        <field name="description">Create a new belt test event/session</field>
        <field name="category">belt</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "Name of the test event"},
        "test_date": {"type": "string", "format": "date", "description": "Date of the test (YYYY-MM-DD)"},
        "location": {"type": "string", "description": "Location of the test"},
        "program_name": {"type": "string", "description": "Program this test is for"},
        "max_participants": {"type": "integer", "description": "Maximum number of participants", "default": 20}
    },
    "required": ["name", "test_date"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create a belt test for March 30th
Schedule a test event for Saturday
Create a new belt test called Spring Grading</field>
        <field name="confirmation_template">Create new belt test "{name}" on {test_date}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_attendance_log_create" model="dojo.ai.intent.schema">
        <field name="intent_type">attendance_log_create</field>
        <field name="name">Create Attendance Entry</field>
        <field name="description">Manually create an attendance log entry</field>
        <field name="category">attendance</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "session_id": {"type": "integer", "description": "ID of the class session"},
        "checkin_datetime": {"type": "string", "format": "date-time", "description": "Check-in time (defaults to now)"},
        "note": {"type": "string", "description": "Optional note"}
    },
    "required": ["member_name", "session_id"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Log attendance for John at 5pm
Create an attendance entry for Sarah
Record Test8's check-in at 3:30pm</field>
        <field name="confirmation_template">Create attendance entry for {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_credit_transaction_create" model="dojo.ai.intent.schema">
        <field name="intent_type">credit_transaction_create</field>
        <field name="name">Create Credit Transaction</field>
        <field name="description">Record a credit transaction (grant, hold, or adjust credits)</field>
        <field name="category">subscription</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "subscription_id": {"type": "integer", "description": "ID of the member subscription"},
        "amount": {"type": "integer", "description": "Number of credits (positive = add, negative = deduct)"},
        "transaction_type": {"type": "string", "enum": ["grant", "hold", "expiry", "adjustment"], "description": "Type of transaction"}
    },
    "required": ["member_name", "amount", "transaction_type"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Add 5 credits to John's account
Deduct 2 credits from Sarah
Adjust Test8's credits for makeup class</field>
        <field name="confirmation_template">{transaction_type} {amount} credits for {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_marketing_campaign_create" model="dojo.ai.intent.schema">
        <field name="intent_type">marketing_campaign_create</field>
        <field name="name">Create Marketing Campaign</field>
        <field name="description">Create a new marketing campaign</field>
        <field name="category">marketing</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "Campaign name"},
        "subject": {"type": "string", "description": "Email subject line"},
        "schedule_type": {"type": "string", "enum": ["one_time", "recurring"], "description": "One-time or recurring campaign"},
        "scheduled_date": {"type": "string", "format": "date", "description": "When to send (YYYY-MM-DD)"},
        "send_email": {"type": "boolean", "description": "Send via email", "default": true},
        "send_sms": {"type": "boolean", "description": "Send via SMS", "default": false}
    },
    "required": ["name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create an email campaign for summer camp
Start a recurring campaign called Monthly Newsletter
Create an SMS blast for active members</field>
        <field name="confirmation_template">Create campaign "{name}"?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_social_post_create" model="dojo.ai.intent.schema">
        <field name="intent_type">social_post_create</field>
        <field name="name">Create Social Media Post</field>
        <field name="description">Create a post for social media</field>
        <field name="category">social</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "message": {"type": "string", "description": "Post message text"},
        "platform": {"type": "string", "enum": ["facebook", "instagram", "both"], "description": "Platform to post on"},
        "scheduled_date": {"type": "string", "format": "date-time", "description": "When to post (optional - draft if not specified)"}
    },
    "required": ["message"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Post about our new program on Facebook
Create an Instagram post about belt testing
Draft a social media post about our schedule</field>
        <field name="confirmation_template">Create social post: "{message}"?</field>
        <field name="is_undoable">False</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_instructor_profile_update" model="dojo.ai.intent.schema">
        <field name="intent_type">instructor_profile_update</field>
        <field name="name">Update Instructor Profile</field>
        <field name="description">Update an instructor's profile information</field>
        <field name="category">member</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "instructor_name": {"type": "string", "description": "Name of the instructor"},
        "name": {"type": "string", "description": "New display name"},
        "bio": {"type": "string", "description": "Short biography"},
        "active": {"type": "boolean", "description": "Whether the profile is active"}
    },
    "required": ["instructor_name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Update John's instructor profile
Set Sarah's speciality to karate
Update Mike's bio to highlight his BJJ experience</field>
        <field name="confirmation_template">Update profile for instructor {instructor_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_emergency_contact_create" model="dojo.ai.intent.schema">
        <field name="intent_type">emergency_contact_create</field>
        <field name="name">Add Emergency Contact</field>
        <field name="description">Add an emergency contact for a member</field>
        <field name="category">member</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "name": {"type": "string", "description": "Name of the emergency contact"},
        "phone": {"type": "string", "description": "Phone number"},
        "relationship": {"type": "string", "description": "Relationship (parent, sibling, etc.)"},
        "email": {"type": "string", "format": "email", "description": "Email address (optional)"},
        "is_primary": {"type": "boolean", "description": "Set as primary contact", "default": false}
    },
    "required": ["member_name", "name", "phone", "relationship"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Add Jane Smith as emergency contact for John
Record his mother's phone number as backup contact
Add Sarah's guardian contact information</field>
        <field name="confirmation_template">Add {name} as emergency contact for {member_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- NEW MODEL CRUD Intents                                              -->
    <!-- ════════════════════════════════════════════════════════════════════ -->

    <record id="intent_martial_art_style_create" model="dojo.ai.intent.schema">
        <field name="intent_type">martial_art_style_create</field>
        <field name="name">Create Martial Art Style</field>
        <field name="description">Add a new martial art style to the system</field>
        <field name="category">system</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "Name of the martial art style (e.g., Brazilian Jiu-Jitsu, Karate)"},
        "code": {"type": "string", "description": "Short code (e.g., BJJ, KAR)"},
        "description": {"type": "string", "description": "Description of the style"}
    },
    "required": ["name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Add Brazilian Jiu-Jitsu as a martial art style
Create a new style called Muay Thai
Add Taekwondo with code TKD</field>
        <field name="confirmation_template">Create martial art style "{name}"?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_subscription_plan_create" model="dojo.ai.intent.schema">
        <field name="intent_type">subscription_plan_create</field>
        <field name="name">Create Subscription Plan</field>
        <field name="description">Create a new subscription plan</field>
        <field name="category">subscription</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "Plan name (e.g., Monthly Unlimited)"},
        "plan_type": {"type": "string", "enum": ["credits", "unlimited", "limited"], "description": "Type of plan"},
        "program_name": {"type": "string", "description": "Program this plan is for"},
        "price": {"type": "number", "description": "Monthly price"},
        "billing_period": {"type": "string", "enum": ["monthly", "quarterly", "biannual", "annual"], "description": "Billing cycle"},
        "description": {"type": "string", "description": "Plan description"}
    },
    "required": ["name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create a monthly unlimited plan for $150
Add a new credits-based plan called 10-Pack
Set up an annual subscription plan for kids BJJ</field>
        <field name="confirmation_template">Create subscription plan "{name}"?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_program_enrollment_create" model="dojo.ai.intent.schema">
        <field name="intent_type">program_enrollment_create</field>
        <field name="name">Enroll Member in Program</field>
        <field name="description">Enroll a member into a martial arts program</field>
        <field name="category">enrollment</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "program_name": {"type": "string", "description": "Name of the program"},
        "enrolled_date": {"type": "string", "format": "date", "description": "Enrollment date (YYYY-MM-DD, defaults to today)"},
        "notes": {"type": "string", "description": "Optional enrollment notes"}
    },
    "required": ["member_name", "program_name"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Enroll John in the Kids BJJ program
Add Sarah to the Adult Karate program
Sign up Test8 for Fundamentals starting today</field>
        <field name="confirmation_template">Enroll {member_name} in {program_name}?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <record id="intent_belt_test_registration_create" model="dojo.ai.intent.schema">
        <field name="intent_type">belt_test_registration_create</field>
        <field name="name">Register for Belt Test Event</field>
        <field name="description">Register a member for a specific belt test event</field>
        <field name="category">belt</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "test_id": {"type": "integer", "description": "ID of the belt test event"},
        "rank_name": {"type": "string", "description": "Target belt rank"},
        "program_name": {"type": "string", "description": "Name of the program"},
        "notes": {"type": "string", "description": "Optional notes"}
    },
    "required": ["member_name", "test_id"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Register John for the Spring belt test
Sign up Sarah for the upcoming grading event
Add Test8 to belt test #5</field>
        <field name="confirmation_template">Register {member_name} for belt test event?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">True</field>
        <field name="active">True</field>
    </record>

    <record id="intent_marketing_card_create" model="dojo.ai.intent.schema">
        <field name="intent_type">marketing_card_create</field>
        <field name="name">Create Marketing Card</field>
        <field name="description">Create a marketing card for kiosk or portal display</field>
        <field name="category">marketing</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "Card name/title"},
        "card_type": {"type": "string", "description": "Type of card"},
        "subtitle": {"type": "string", "description": "Card subtitle"},
        "body": {"type": "string", "description": "Card body content"},
        "publish_kiosk": {"type": "boolean", "description": "Show on kiosk", "default": true},
        "publish_portal": {"type": "boolean", "description": "Show on member portal", "default": false}
    },
    "required": ["name"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create a marketing card for summer camp
Add a promo card for the kiosk about new classes
Create a portal card about our belt testing schedule</field>
        <field name="confirmation_template">Create marketing card "{name}"?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_kiosk_announcement_create" model="dojo.ai.intent.schema">
        <field name="intent_type">kiosk_announcement_create</field>
        <field name="name">Create Kiosk Announcement</field>
        <field name="description">Create an announcement to display on the kiosk</field>
        <field name="category">system</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "title": {"type": "string", "description": "Announcement title"},
        "body": {"type": "string", "description": "Announcement content"},
        "active": {"type": "boolean", "description": "Whether announcement is active", "default": true}
    },
    "required": ["title", "body"]
}</field>
        <field name="allowed_roles">admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Create a kiosk announcement about holiday hours
Post a notice about the upcoming tournament
Add an announcement about schedule changes</field>
        <field name="confirmation_template">Create kiosk announcement "{title}"?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

    <record id="intent_course_auto_enroll_create" model="dojo.ai.intent.schema">
        <field name="intent_type">course_auto_enroll_create</field>
        <field name="name">Set Up Auto-Enrollment</field>
        <field name="description">Create an auto-enrollment rule for a member in a class template</field>
        <field name="category">enrollment</field>
        <field name="parameters_schema">{
    "type": "object",
    "properties": {
        "member_name": {"type": "string", "description": "Name of the member"},
        "class_name": {"type": "string", "description": "Name of the class template"},
        "mode": {"type": "string", "enum": ["all", "next_only"], "description": "Auto-enroll in all future sessions or just the next one", "default": "all"}
    },
    "required": ["member_name", "class_name"]
}</field>
        <field name="allowed_roles">instructor,admin</field>
        <field name="requires_confirmation">True</field>
        <field name="example_phrases">Auto-enroll John in all Fundamentals classes
Set up Sarah for automatic enrollment in Kids BJJ
Enable auto-enrollment for Test8 in Advanced Sparring</field>
        <field name="confirmation_template">Set up auto-enrollment for {member_name} in {class_name} ({mode})?</field>
        <field name="is_undoable">True</field>
        <field name="supports_bulk">False</field>
        <field name="active">True</field>
    </record>

</odoo>
