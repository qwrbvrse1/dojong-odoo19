# This file is intentionally empty.
# dojo_sign no longer adds a per-plan sign.template field — waiver configuration
# is now global (see dojo.waiver.config) and signing is handled inline in the
# onboarding wizard without requiring Odoo Enterprise Sign.
