<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Per-plan waiver template removed: waiver configuration is now global
        (dojo.waiver.config singleton) and does not require Odoo Enterprise Sign.
        This file intentionally left empty to avoid breaking any dependent XPath.
    -->
</odoo>
