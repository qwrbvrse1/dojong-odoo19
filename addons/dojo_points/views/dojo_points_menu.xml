<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ─── Root menu (under Dojo core) ─────────────────────────────────── -->
    <menuitem
        id="menu_dojo_points_root"
        name="Points"
        sequence="70"
        groups="dojo_base.group_dojo_instructor"/>

    <!-- ─── Points Ledger ────────────────────────────────────────────────── -->
    <menuitem
        id="menu_dojo_points_ledger"
        name="Points Ledger"
        parent="menu_dojo_points_root"
        action="dojo_points.action_dojo_points_transaction"
        sequence="10"
        groups="dojo_base.group_dojo_instructor"/>

    <!-- ─── Points Settings (admin only) ────────────────────────────────── -->
    <menuitem
        id="menu_dojo_points_settings"
        name="Points Settings"
        parent="menu_dojo_points_root"
        action="dojo_points.action_dojo_points_config"
        sequence="20"
        groups="dojo_base.group_dojo_admin"/>
</odoo>
