<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add Events smart button to dojo.member form -->
    <record id="view_dojo_member_form_event_inherit" model="ir.ui.view">
        <field name="name">dojo.member.form.event.inherit</field>
        <field name="model">dojo.member</field>
        <field name="inherit_id" ref="dojo_core.view_dojo_member_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    name="action_view_event_registrations"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-ticket"
                    invisible="event_registration_count == 0"
                >
                    <field name="event_registration_count" widget="statinfo" string="Events"/>
                </button>
            </xpath>
            <!-- hidden field required for button visibility expression -->
            <xpath expr="//sheet" position="inside">
                <field name="event_registration_count" invisible="1"/>
            </xpath>
        </field>
    </record>
</odoo>
